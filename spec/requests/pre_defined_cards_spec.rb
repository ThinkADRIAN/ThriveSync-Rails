require 'rails_helper'

RSpec.describe "PreDefinedCards", type: :request do
  describe "GET /pre_defined_cards" do
    it "works! (now write some real specs)" do
      #get pre_defined_cards_path
      #guardexpect(response).to have_http_status(200)
    end
  end
end
