<%= form_for(@passive_workout) do |f| %>
  <% if @passive_workout.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@passive_workout.errors.count, "error") %> prohibited this passive_workout from being saved:</h2>

      <ul>
      <% @passive_workout.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :passive_data_point_id %><br>
    <%= f.number_field :passive_data_point_id %>
  </div>
  <div class="field">
    <%= f.label :workout_type %><br>
    <%= f.text_field :workout_type %>
  </div>
  <div class="field">
    <%= f.label :kcal_burned_value %><br>
    <%= f.text_field :kcal_burned_value %>
  </div>
  <div class="field">
    <%= f.label :kcal_burned_unit %><br>
    <%= f.text_field :kcal_burned_unit %>
  </div>
  <div class="field">
    <%= f.label :distance_value %><br>
    <%= f.text_field :distance_value %>
  </div>
  <div class="field">
    <%= f.label :distance_unit %><br>
    <%= f.text_field :distance_unit %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
